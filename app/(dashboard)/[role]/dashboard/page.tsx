import React from 'react';

export default function DashboardPage({ params }: { params: { role: string } }) {
  return <section className="flex justify-center">Selamat Datang di Dashboard, Admin!</section>;
}
